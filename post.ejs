<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/styles.css">
    <style>
        body {
            background-color: #ffebef; /* Light pink background */
            font-family: 'Work Sans', sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }

        .post-content {
            max-width: 600px;
            padding: 20px;
            background-color: white;
            border-radius: 10px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
            margin-bottom: 20px;
        }

        .blog-title {
            font-size: 2rem;
            margin-bottom: 10px;
            color: #333;
        }

        .author-info {
            font-size: 1rem;
            color: #666;
            margin-bottom: 10px;
        }

        .back-btn {
            background-color: #28a745; /* Green color */
            color: white;
            padding: 0.5rem 1rem;
            text-decoration: none;
            border-radius: 5px;
            transition: background-color 0.3s, box-shadow 0.3s;
        }

        .back-btn:hover {
            background-color: #218838;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
    </style>
    <title><%= post.title %> - Personal Blog</title>
</head>
<body>
    <div class="post-content">
        <h1 class="blog-title"><%= post.title %></h1>
        <p class="author-info"><b>Author:</b> Ronak</p>
        <p><%= post.content %></p>
    </div>
    <a class="back-btn" href="/">Back to Blog</a>
</body>
</html>
